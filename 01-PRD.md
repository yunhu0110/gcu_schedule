# 01. 제품 기획서 (PRD)

## 1. 배경
6명이 고정으로 함께하는 소모임. 지금까지 일정 조율은 카톡에서 "나는 이번 주 토요일 안 돼"가
스크롤에 묻히는 방식이었고, 모임 기록(다녀온 곳, 사진, 정산)은 남지 않는다.
매달 모임장이 바뀌는 구조가 이미 있으므로, **모임장을 앱의 중심 화면으로 세우고**
그 주변에 일정 조율과 기록을 붙인다.

## 2. 목표 / 비목표
### 목표
- 6명의 가용 날짜를 한 화면에서 겹쳐 보고, 모임 날짜를 3분 안에 확정한다.
- 이번 달 모임장이 누구인지 앱을 열면 0초 만에 안다.
- 모임 기록이 위키로 쌓여 1년 뒤에 "작년 8월에 어디 갔었지?"가 검색된다.

### 비목표
- 신규 유입, 공개 모임 탐색, 6인 초과 확장
- 스토어 심사 통과 / 수익화 / 광고
- 실시간 채팅 (카톡을 대체하지 않는다. 앱은 **기록과 조율** 담당)

## 3. 사용자
6명 모두 동등한 멤버. 권한 차이는 딱 두 가지뿐:
- **이번 달 모임장**: 해당 월 모임의 날짜 확정, 장소 등록, 정산 마감, 표지 편집 권한
- **관리자(1명, 개발자 본인)**: 초대 코드 발급, 모임장 로테이션 편집, 멤버 비활성화

## 4. 기능 명세

### F1. 이번 달 모임장 표지 ★핵심
- 홈 최상단 전면. 모임장 사진 + 이름 + `Vol. 2026.07` + 모임장이 남긴 한마디.
- 다음 달로 넘어가면 표지가 교체되고, 지난 표지는 "지난 호" 아카이브로 남는다.
- 모임장 로테이션은 연간 테이블로 미리 지정(수동 편집 가능). 순서 자동 생성 옵션 제공.
- 모임장은 본인 차례 달에 표지 문구 / 테마 색상 / 한마디를 편집할 수 있다.

### F2. 가용성 달력 ★핵심
- 월 그리드. 각 날짜에 본인 상태를 `가능 / 불가 / 미정`으로 표시.
- 입력: 날짜 탭으로 상태 순환(가능→불가→해제), 롱프레스 후 드래그로 범위 지정.
- 집계 뷰: 날짜별로 "가능 6/6", "가능 4/6 (불가 2)"를 색 농도로 표현.
- **전원 가능한 날**은 특별 강조. 없으면 "불가 0명 + 가능 최다" 날짜를 추천으로 제시.
- 날짜별 상세 시트: 누가 가능/불가/미정인지, 각자 남긴 메모("오후만 가능" 등).
- 반복 불가 규칙: "매주 수요일 불가" 같은 상시 조건 등록 → 달력에 자동 반영.
- 입력 마감: 매월 지정일(기본 25일)까지 다음 달 입력 요청. 미입력자에게 리마인더.

### F3. 모임 (Meetup)
- 상태: `제안 → 날짜 투표 → 확정 → 완료`(또는 `취소`)
- 확정된 모임은 홈에 D-day 카드로 표시. 장소, 시간, 예상 회비, 참석 여부.
- 완료 시 자동으로 위키에 회차 문서가 생성된다(템플릿 적용).

### F4. 위키 (컨플루언스 느낌)
- 스페이스 개념 없이 **최대 2단계 페이지 트리** (상위 페이지 → 하위 페이지).
- 마크다운 기반 문서. 이미지 첨부, 체크리스트, 표.
- 리비전 저장 + "누가 언제 수정" 표시. 이전 버전 보기 / 복원.
- 문서 하단 댓글.
- 전체 텍스트 검색 (Postgres full-text search).
- 기본 템플릿: `모임 회차 기록`, `가고 싶은 곳 후보`, `회칙`, `정산 내역`.

### F5. 정산
- 모임별로 결제자 / 총액 / 인원 분배 입력 → 1인당 금액 자동 계산.
- 각자 "송금 완료" 체크. 미정산자 표시.
- 회비(있다면) 월별 납부 현황 보드.

### F6. 참석 기록 & 뱃지
- 모임별 참석/불참 기록 누적. 연간 참석률.
- 가벼운 뱃지: `개근`, `모임장 완수`, `총무` 등. 게임화는 딱 이 정도까지.

### F7. 알림
- 새 모임 제안 / 날짜 확정 / 가용성 입력 마감 D-3 / 새 위키 문서 / 정산 요청
- Expo push. 항목별 on/off 설정.

### F8. 가입 & 정원
- 관리자가 발급한 **초대 코드**로만 가입. 코드는 1회용.
- 활성 멤버 6명 도달 시 가입 차단 (DB 트리거).
- 로그인: 이메일 + 비밀번호 (또는 Apple/Google 로그인). 개인정보는 닉네임·프로필 이미지만.

## 5. 우선순위
| 순위 | 범위 |
|---|---|
| P0 (없으면 앱이 아님) | F8 가입, F1 모임장 표지, F2 가용성 달력, F3 모임 확정 |
| P1 (1차 릴리즈 목표) | F4 위키, F7 알림 |
| P2 | F5 정산, F6 참석 기록/뱃지 |
| P3 | 지난 호 아카이브 연출, 지도 위 방문 기록 핀, 사진 갤러리 |

## 6. 성공 기준
- 6명 전원이 매달 25일 전에 다음 달 가용성을 입력한다.
- 카톡에서 날짜 조율 대화가 사라진다.
- 3개월 후에도 앱을 지운 사람이 없다.
