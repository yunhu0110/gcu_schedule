# 10. 디자인 브리프 — 월간gcu (디자이너/디자인툴 핸드오프용)

> **이 문서 하나로 화면을 그릴 수 있게** 추린 자체 완결형 브리프.
> 원본은 `01-PRD`, `03-UI-UX-GUIDE`, `04-SCREENS`. 값이 충돌하면 이 문서가 최신.
> 결과물은 iOS/Android용 모바일 앱(세로 화면). 최소 지원 폭 **320pt**.

---

## 0. 한 장 요약
- **무엇**: 6명 고정 소모임용 모바일 **위키 + 일정 조율** 앱. 이름 **월간gcu**.
- **컨셉**: **「월간지」**. 매달 모임장(=편집장)이 바뀌고, 앱을 열면 그 달 **표지**가 전면에 뜬다.
  `VOL. 2026.07` 같은 호수 표기가 앱 전체의 뼈대. 컨플루언스의 정보 구조 + 인쇄 잡지의 표지 감성.
- **사용자**: 정확히 6명. 모두 동등. 권한 차이는 (1) 이번 달 모임장 (2) 관리자 1명뿐.
- **톤**: 인쇄물처럼 정제됨. 헤어라인(괘선), 절제된 명조 대제목, 모노 숫자. 촌스러움 = 최대 실패.
- **하단 탭 5개**: `표지` · `달력` · `위키` · `정산` · `나`

---

## 1. 디자인 원칙
1. **입력은 한 손으로 3초.** 달력 날짜 탭 1회=가능, 2회=불가, 3회=해제.
2. **그림자 대신 1px 헤어라인**(`ink12`)으로 구획을 나눈다. 인쇄물의 괘선 감각.
3. **명조는 표지와 화면 대제목에만.** 본문에 명조 섞으면 컨셉이 무너진다.
4. **숫자·날짜·금액·호수는 전부 모노(Mono)**로. 잡지의 캡션 감성.
5. **애니메이션은 딱 하나** — 콜드 스타트 시 표지 제목 마스크 리빌 1회(600ms). 그 외 화면엔 없음.
6. **6이라는 숫자를 시각 언어로.** 채도로 뭉개지 말고 **칸을 센다**(6칸 게이지).

### ❌ 이렇게는 안 만든다 (안티패턴)
- 카드가 둥둥 떠 있는 무색 대시보드 / 그라데이션 히어로
- 숫자 크게 + 라벨 작게 조합 / 이모지로 성격을 대신하는 화면
- 과한 라운드, 남발하는 드롭섀도우

---

## 2. 디자인 토큰 (그대로 사용, 이 밖의 색 금지)

### 색 (7개 + 파생 투명도)
| 토큰 | HEX | 용도 |
|---|---|---|
| `ink` | `#14161D` | 본문 텍스트, 헤어라인, **불가** 상태 |
| `paper` | `#FFFFFF` | 기본 배경 |
| `mist` | `#EDECE8` | 판면(섹션 배경), **미입력** 상태 |
| `cobalt` | `#2140E0` | 주 액션 버튼, **가능** 상태 |
| `neon` | `#E8318A` | 표지 강조, **전원 가능** 날짜 |
| `amber` | `#FFC53D` | **미정** 상태, 마감 임박 경고 |
| `slate` | `#7A7F8C` | 보조 텍스트, 캡션 |
- 투명도는 `ink12`, `ink08`처럼 파생. 다크모드는 나중(P3) — 지금 light만.
- 모임장이 고르는 `theme_color`는 **표지 영역 안에서만** 적용. 앱 전체 색은 안 바뀐다.

### 타이포그래피
| 역할 | 서체 | 사용처 |
|---|---|---|
| **Display** | 국문 명조 극태 (`Noto Serif KR` 900) | 표지 이름, 화면 대제목 — **절제** |
| **Body** | `Pretendard` 400/600 | 모든 본문·버튼·라벨 |
| **Mono** | `IBM Plex Mono` 500 | 호수(`VOL. 2026.07`)·날짜·D-day·금액·카운터 |

스케일: `display 44/1.15` · `h1 26/1.3` · `h2 20/1.35` · `body 16/1.5` · `bodySm 14/1.5` · `caption 12/1.4` · `mono 12/1.2 (자간 1.2, 대문자)`

### 레이아웃
- 8pt 그리드. 화면 좌우 여백 **20**. 섹션 간격 **32**.
- 라운드: 카드 **4**, 버튼 **8**, 표지 **0**(full-bleed).
- 섹션 머리 = 모노 대문자 눈썹 라벨 + 좌측 얇은 잉크 바.

---

## 3. 시그니처 요소 (이 둘을 못 살리면 앱이 안 산다)

### 3-1. 표지 (홈 상단, 화면 높이 ~70%, full-bleed)
```
┌─────────────────────────────┐
│ V │                         │  ← 좌측 세로 모노 호수 (VOL. 2026.07)
│ O │   [모임장 사진 · 흑백    │
│ L │    + theme_color 오버레이]│
│ . │                         │
│ 0 │   이  달  의            │  ← 명조 극태, 2줄 좌측 정렬
│ 7 │   모 임 장              │
│   │   김 ○ ○                │
│   │  ─────────────────      │
│   │  "8월엔 성심당 재방문."  │  ← 모임장 한마디
└─────────────────────────────┘
```
- 사진은 **흑백 처리 후 `theme_color` 멀티플라이 오버레이** → 6명 사진 화질이 달라도 톤 통일.
- 콜드 스타트 시 표지 제목 **마스크 리빌 1회(600ms)**. `prefers-reduced-motion`이면 생략.
- 이번 달 모임장이 본인이면 우상단 `편집` 아이콘.

### 3-2. 6칸 게이지 달력 셀
```
 ┌──────┐   ┌──────┐   ┌──────┐
 │  14  │   │  15  │   │  16  │
 │▓▓▓▓▓▓│   │▓▓▓▓░░│   │▓▓██░░│
 └──────┘   └──────┘   └──────┘
  전원가능    가능4/미입력2  불가2 포함
```
- 셀 하단 6칸 미니 게이지 색: `cobalt`=가능, `ink`=불가, `amber`=미정, `mist`=미입력.
- **전원 가능**한 날은 셀 전체를 `neon`으로 채우고 숫자 흰색 반전. 한눈에 튀게.
- 불가가 1명이라도 있으면 `ink` 칸이 보여 색약 사용자도 위치로 구분 가능.
- 반복 규칙에서 온 불가는 **빗금 패턴**으로 살짝 다르게.

---

## 4. 공통 컴포넌트 인벤토리 (먼저 만들면 화면이 빨라진다)
| 컴포넌트 | 설명 / 상태 |
|---|---|
| `Text` | 위 타이포 역할별 프리셋 (display/h1/h2/body/bodySm/caption/mono) |
| `Button` | primary(`cobalt` 채움) / secondary(헤어라인 아웃라인) / ghost. 상태: 기본/눌림/비활성/로딩. 터치 44x44 |
| `Card` | 라운드 4, 배경 `paper`, 구획은 헤어라인 |
| `Sheet` | 하단 시트 (날짜 상세, 범위 상태 지정, 확인 다이얼로그) |
| `Avatar` | 원형, 프로필 이미지 / 이니셜 폴백. 크기 여러 종. 미입력자 표시용 흐림 상태 |
| `GaugeCell` | 3-2 달력 셀. 4상태 게이지 + 전원가능 반전 |
| `SectionHeader` | 모노 대문자 눈썹 라벨 + 좌측 잉크 바 |
| `Badge` | 상태 배지(모임 상태: 제안/투표/확정/완료/취소), 뱃지(개근/모임장 완수 등) |
| `DdayPill` | 모노, `D-19` 형태 |
| `Skeleton` | 로딩용, 판면 색 `mist`. **스피너 대신 스켈레톤** |
| `EmptyState` | 빈 화면 = 초대 문구 ("아직 아무도 8월 일정을 넣지 않았어요. 첫 번째로 넣어볼까요?") |
| `Toast` | 낙관적 업데이트 실패/성공 알림 |

---

## 5. 화면 명세 (★우선순위 = 먼저 그려주세요)

### 우선순위
- **P0 (가장 먼저)**: 인증(로그인·가입) → S1 표지 → S2 달력 → S3 모임
- **P1**: S4/S5 위키
- **P2**: S7 정산, S8 나
- **P3**: S6 과월호 서가, S9 모임장 로테이션(관리자)

---

### ★ 인증 (P0)
- **sign-in**: 이메일 + 비밀번호. 하단에 "초대 코드로 시작하기" 링크. 자동 로그인.
- **join**: 초대코드 입력 → 검증 → 이메일/비번 설정 → 닉네임/프로필사진 → 홈.
- 코드 무효/정원 초과 시 이유를 문구 그대로 노출.
- 표지 컨셉을 로그인에서도 살짝 예고하면 좋음(호수 표기, 명조 타이틀).

### ★ S1. 표지 (홈) (P0)
```
┌──────────────────────────────┐
│  [ 표지 (3-1 참조, 화면 70%) ] │
├──────────────────────────────┤
│ NEXT MEETUP        (눈썹라벨)  │
│ 8월 15일 (토) 18:00     D-19  │  ← 확정 시
│ 성심당 본점 · 1인 25,000원     │
│ [ 참석 ]  [ 불참 ]            │
├──────────────────────────────┤
│ 8월 일정 입력                  │
│ ▓▓▓▓░░ 4명 입력 · 마감 D-3    │  ← 미입력자 아바타 노출
│ [ 내 일정 입력하기 ]          │
├──────────────────────────────┤
│ RECENT PAGES                  │
│ · 7월 모임 기록 (어제)         │
│ · 가고 싶은 곳 후보 (3일 전)   │
└──────────────────────────────┘
```
- **모임 미확정 상태**: 두 번째 블록이 "날짜 조율 중 · 후보 3개"로 바뀌고 투표 CTA.
- 필요한 상태 버전: (a) 모임 확정 (b) 조율 중 (c) 아직 모임 없음.

### ★ S2. 달력 (P0)
- 상단: 월 이동(스와이프+화살표), 우측 세그먼트 `내 입력 / 전체 보기`.
- 본문: **6칸 게이지 월 그리드** (3-2). 주 시작 **일요일**, 앞뒤 빈칸은 이전/다음 달 흐리게.
- 하단 고정 바: `전원 가능한 날 2개 · 8/15, 8/22` → 탭 시 해당 날짜로 스크롤.
- 인터랙션: 날짜 탭=상태 순환. 롱프레스=범위 선택 모드 → 선택 후 상태 일괄 지정 시트.
- 날짜 상세 시트(전체 보기): 멤버별 상태+메모, "이 날로 확정"(모임장만).
- 상단 배너: 다음 달 미입력 시 "9월 일정 아직 안 넣었어요"(마감 7일 전부터).

### ★ S3. 모임 상세 (P0)
- 헤더: 회차/월, 상태 배지, 모임장 아바타.
- `voting`: 후보 날짜 카드 목록 + 후보별 가능 인원 게이지 + 1~3순위 투표.
- `confirmed`: 날짜/시간/장소(지도 링크)/예상 회비/참석자 목록.
- `done`: 연결된 위키 기록 바로가기 + 정산 요약.
- 하단 댓글. 모임장 액션: 후보 추가/날짜 확정/장소 등록/완료 처리.

### S4. 위키 목록 (P1)
- 상단 검색 바(전문 검색). 고정 문서 별도 섹션 상단. **2단계 트리** 접기/펼치기.
- 각 항목에 최종 수정자 아바타 + 상대 시간. FAB: 새 문서(템플릿 선택).

### S5. 문서 상세 (P1)
- 읽기 모드 기본(마크다운: 제목/목록/체크박스/표/이미지/인용). 우상단 `편집`(풀스크린+툴바).
- 하단: 수정 이력(버전 목록→이전 버전 보기/복원), 댓글.

### S7. 정산 (P2)
- 진행 중 정산 카드(모임별): 총액/1인당/내 상태(보냄·안 보냄). `송금 완료` 체크.
- 미정산 인원은 **이름 그대로 노출**(6명이라 이게 최고의 리마인더). 하단: 연간 지출/월별 막대.

### S8. 나 (P2)
- 프로필(닉네임/사진), 상시 불가 요일, 알림 설정. 내 통계(참석률/모임장 횟수/뱃지). 로그아웃/버전.

### S6. 과월호 서가 (P3)
- 지난 표지 2열 그리드 썸네일 → 탭하면 그 달 표지+기록+사진. 연도별 구분. **정서적 보상 지점.**

### S9. 모임장 로테이션 (관리자) (P3)
- 12개월 리스트에 멤버 배정, 드래그 순서 변경. `자동 배정`. 다음 달 비면 관리자에게만 경고 배너.

---

## 6. 인터랙션 · 문구 규칙
- 상태 변경은 **낙관적 업데이트** 후 서버 반영. 실패 시 되돌리고 토스트.
- **파괴적 동작만 확인 시트**(날짜 확정, 정산 마감). 나머지는 즉시 실행 + 되돌리기.
- 햅틱: 상태 토글=`selection`, 확정=`success`. 그 외 없음.
- 버튼은 **일어날 일을 그대로**: "제출" ❌ → "날짜 확정하기" ⭕. 확정 토스트도 "확정했어요".
- 시스템 용어 금지: "동기화 실패" ❌ → "저장하지 못했어요. 다시 눌러주세요" ⭕.
- 존댓말 평서문, 문장부호 최소, 감탄사·이모지 남용 금지.

---

## 7. 품질 하한선 (예외 없음 · 접근성)
- 최소 지원 폭 **320pt**까지 안 깨짐. 모든 터치 타깃 **44x44** 이상.
- 텍스트 대비 **4.5:1** 이상. `slate` on `mist`는 캡션에만.
- 이미지엔 `accessibilityLabel`. 달력 셀 라벨: "8월 14일, 6명 모두 가능" 형태.
- 로딩은 스켈레톤(`mist`).

---

## 8. 디자인에 요청하는 산출물
1. **디자인 토큰 확정** — 위 색/타이포/스페이싱을 스타일 스타일가이드/변수로 (개발 `tokens.ts`와 1:1 매칭).
2. **공통 컴포넌트 라이브러리** (§4) — 상태 포함.
3. **P0 화면 목업** — 인증 2종 + S1(3상태) + S2(내입력/전체보기, 상세시트) + S3(voting/confirmed/done).
   - 각 화면 **빈 상태 / 로딩(스켈레톤) / 에러** 버전 포함.
4. **시그니처 2종 정밀 시안** — 표지(흑백+theme_color 오버레이, 리빌 컨셉) / 6칸 게이지 셀 4상태 + 전원가능 반전.
5. (여유되면) P1~P2 화면.

> **첫 배치 추천**: 토큰 → 컴포넌트 → 표지(S1) → 6칸 게이지 달력(S2). 이 두 시그니처가 앱 인상을 결정한다.
