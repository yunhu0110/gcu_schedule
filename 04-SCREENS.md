# 04. 화면 명세

라우팅은 `expo-router` 파일 기반. 하단 탭 5개.

```
app/
  (auth)/sign-in.tsx        로그인
  (auth)/join.tsx           초대 코드 가입
  (tabs)/index.tsx          S1 표지(홈)
  (tabs)/calendar.tsx       S2 달력
  (tabs)/wiki/index.tsx     S4 위키 목록
  (tabs)/wiki/[id].tsx      S5 문서 상세
  (tabs)/settle.tsx         S7 정산
  (tabs)/me.tsx             S8 나
  meetup/[id].tsx           S3 모임 상세
  archive/index.tsx         S6 과월호 서가
  admin/hosts.tsx           S9 모임장 로테이션 (관리자)
```

## S1. 표지 (홈)
```
┌──────────────────────────────┐
│ ▓▓▓▓  이 달 의  모 임 장  ▓▓ │  표지 (전면, 화면 높이 70%)
│ ▓▓▓▓  김 ○ ○              ▓ │
│ ▓▓▓▓  "8월엔 성심당 재방문" ▓ │
├──────────────────────────────┤
│ NEXT MEETUP                  │  모노 눈썹 라벨
│ 8월 15일 (토) 18:00     D-19 │  ← 확정 시. 미확정이면 아래 상태 표시
│ 성심당 본점 · 1인 25,000원    │
│ [ 참석 ]  [ 불참 ]            │
├──────────────────────────────┤
│ 8월 일정 입력                 │
│ ▓▓▓▓░░ 4명 입력 · 마감 D-3    │  ← 미입력자 아바타 표시
│ [ 내 일정 입력하기 ]          │
├──────────────────────────────┤
│ RECENT PAGES                 │
│ · 7월 모임 기록 (어제)        │
│ · 가고 싶은 곳 후보 (3일 전)  │
└──────────────────────────────┘
```
- 모임 미확정 상태에서는 두 번째 블록이 "날짜 조율 중 · 후보 3개"로 바뀌고 투표 CTA가 뜬다.
- 이번 달 모임장이 본인이면 표지 우상단에 `편집` 아이콘.

### 현재 구현 (위 스케치와 다른 부분)
- 헤더(로고·월간GCU·배포일·알림벨) → 담당자 카드 → **모임 히어로(월 페이지)** → 메모장 순.
- 히어로는 **좌우 스와이프로 달 이동**(지난 6개월 ~ 다음 3개월, 기본 = 다음 달).
  넘긴 달에 맞춰 담당자 카드도 함께 바뀌고, 담당자 지정/날짜 확정도 그 달에 적용된다.
- 담당자·확정 날짜는 월별로 나눠 조회하지 않고 `listMonthlyPosts()` / `listPolls()`로 한 번에 받는다
  (행이 몇십 개뿐이라 달을 넘길 때 로딩이 없어야 자연스럽다).
- 날짜 확정 팝업에는 **"N월 모임 날짜 초기화"**(그 달만 미정으로 되돌리기)가 있다. 담당자·관리자만.

## S2. 달력
- 상단: 월 이동(스와이프 + 화살표), 우측에 `내 입력 / 전체 보기` 세그먼트.
- 본문: 6칸 게이지 월 그리드 (03-UI-UX-GUIDE 참조).
- 하단 고정 바: `전원 가능한 날 2개 · 8/15, 8/22` → 탭하면 해당 날짜로 스크롤.
- 날짜 탭 → 상태 순환. 롱프레스 → 범위 선택 모드(선택 후 상태 일괄 지정 시트).
- 날짜 롱탭(전체 보기 모드) → 상세 시트: 멤버별 상태 + 메모, "이 날로 확정"(모임장만).
- 상단 배너: 다음 달 미입력 시 "9월 일정 아직 안 넣었어요" (마감 7일 전부터).

## S3. 모임 상세
- 헤더: 회차/월, 상태 배지, 모임장 아바타.
- `voting` 상태: 후보 날짜 카드 목록 + 각 후보별 가능 인원 게이지 + 1~3순위 투표.
- `confirmed`: 날짜/시간/장소(지도 링크)/예상 회비/참석자 목록.
- `done`: 연결된 위키 기록 문서 바로가기, 정산 요약.
- 하단 댓글.
- 모임장 액션: 후보 추가, 날짜 확정, 장소 등록, 모임 완료 처리.

## S4. 위키 목록
- 상단 검색 바(전문 검색).
- 고정 문서(`is_pinned`)를 상단에 별도 섹션으로.
- 2단계 트리, 접기/펼치기. 각 항목에 최종 수정자 아바타 + 상대 시간.
- FAB: 새 문서 → 템플릿 선택(빈 문서 / 모임 기록 / 후보 목록 / 회칙).

## S5. 문서 상세
- 읽기 모드 기본. 마크다운 렌더링(제목, 목록, 체크박스, 표, 이미지, 인용).
- 우상단 `편집`. 편집은 별도 풀스크린, 마크다운 + 툴바(굵게/목록/체크박스/이미지).
- 저장 시 `version + 1`, 리비전 자동 적재.
- 하단: 수정 이력(누가 언제, 버전 목록 → 이전 버전 보기/복원), 댓글.
- 동시 편집 충돌: 저장 시 서버 `version` 비교 → 다르면 "다른 사람이 먼저 수정했어요" + 비교 화면.

## S6. 과월호 서가
- 지난 표지들을 2열 그리드 썸네일로. 탭하면 그 달의 표지 + 모임 기록 + 사진.
- 연도별 구분. 이 앱의 정서적 보상 지점.

## S7. 정산
- 진행 중인 정산 카드(모임별): 총액, 1인당, 내 상태(보냄/안 보냄).
- `송금 완료` 체크. 결제자에게는 수령 확인 체크.
- 미정산 인원은 이름 그대로 노출한다(6명이니 이게 가장 효과적인 리마인더).
- 하단: 연간 지출 합계, 월별 막대.

## S8. 나
- 프로필(닉네임, 사진), 상시 불가 요일 설정, 알림 설정.
- 내 통계: 참석률, 모임장 수행 횟수, 뱃지.
- 로그아웃 / 앱 버전 / 업데이트 확인(EAS Update 수동 체크 버튼).

## S9. 모임장 로테이션 (관리자)
- 12개월 리스트에 멤버 배정. 드래그로 순서 변경.
- `자동 배정`: 활성 멤버를 균등 순환. 이미 배정된 달은 건드리지 않는다.
- 다음 달 모임장이 비어 있으면 홈 상단에 관리자에게만 경고 배너.

## 인증 흐름
1. 최초 실행 → `sign-in`. 하단에 "초대 코드로 시작하기".
2. `join`: 코드 입력 → 검증(Edge Function) → 이메일/비밀번호 설정 → 닉네임/사진 → 홈.
3. 코드가 유효하지 않거나 정원이 찼으면 그 이유를 그대로 알린다.
4. 세션은 `expo-secure-store`에 보관. 자동 로그인.
