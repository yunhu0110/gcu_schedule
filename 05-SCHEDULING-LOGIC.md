# 05. 일정 조율 로직

## 1. 상태 정의 (2026-07 개정 — "등록되는 건 가능뿐")
| 상태 | 의미 | 저장 |
|---|---|---|
| `available` | 가능 | 행 존재 |
| 미등록 | 안 되거나, 아직 답 안 함 | **행 없음** |

- 달력 입력 팝업의 **불가 버튼은 상태 저장이 아니라 삭제 동작**이다.
  그 기간에 내가 등록해둔 `available` 행을 지워서 "미등록"으로 되돌린다.
- 그래서 불가 사유·시간대 입력이 없다. 불가는 남는 기록이 아니다.
- `unavailable` / `maybe`는 스키마에는 남아 있지만(과거 데이터 호환) 앱에서 새로 쓰지 않고,
  집계·화면에서도 '가능'으로 세지 않는다.

> 개정 이유: "미입력 = 가능(default 가능)"으로 접었더니 아무도 입력하지 않은 달이
> 전원 가능으로 칠해져 달력이 통째로 하이라이트됐다. 등록된 가능만 세는 쪽으로 되돌린다.

## 2. 유효 상태 계산 (개별 입력 > 반복 규칙)
```
effectiveStatus(member, date):
  if availabilities[member][date] 존재 → 그 값
  else if recurring_blocks에 date의 요일이 active → 'unavailable' (출처: rule)
  else → 미입력
```
반복 규칙에서 온 `unavailable`은 UI에서 살짝 다르게 표기한다(빗금 패턴).

## 3. 집계 · 달력 하이라이트
날짜별로 `available / unavailable / maybe / missing` 4개 카운트. 합은 항상 활성 멤버 수(6).
화면이 쓰는 값은 `available` 하나이고, 나머지는 게이지의 회색 칸(미등록)으로 합쳐 보인다.

6칸 게이지의 **가능 칸은 그 멤버의 프로필 색**으로 칠한다(멤버당 1/6칸).
등록된 순서대로 앞에서부터 채우고 따로 정렬하지 않는다. 전원 가능하면 여섯 칸이 각자 색으로 꽉 찬다.

셀 배경(연두, 가능 인원이 많을수록 진하게) — 토큰은 `theme/tokens.ts`:
| 가능 인원 | 배경 |
|---|---|
| 전원(6) | `availAll` |
| 5 (1명 빠짐) | `availHigh` — 채도 down |
| 4 (2명 빠짐) | `availMid` — 채도 더 down |
| 3 이하 | 하이라이트 없음 (게이지만) |

## 4. 달력 하단 "가능한 날" 목록 (현재 구현)
가능 1명 이상인 그 달의 날짜를 **가능 인원 내림차순**, 동점이면 빠른 날짜 순으로 최대 10개.
불가라는 상태가 없으므로 탈락 조건도 없다.

## 4-1. 추천 날짜 알고리즘 (참고 — 미구현 스펙)
```
score(date) =
    available * 10
  + maybe * 3
  - unavailable * 100      // 불가 1명은 사실상 탈락
  - missing * 1            // 정보 부족 페널티
  + (isWeekend(date) ? 5 : 0)
  + (hostAvailable ? 20 : -1000)   // 모임장 없는 모임은 성립 불가
```
1. `unavailable == 0 && missing == 0`인 날짜를 1순위 그룹으로.
2. 그룹 내에서 `score` 내림차순, 동점이면 빠른 날짜 우선.
3. 1순위 그룹이 비면 `unavailable == 0`인 날짜(미입력 존재) → 그다음 `unavailable == 1`.
4. 상위 3개를 후보로 제시. **자동 확정은 하지 않는다.** 확정은 항상 모임장의 명시적 동작.

## 5. 입력 사이클
- 매월 `1일`: 다음 달 달력 오픈.
- 매월 `25일`(설정 가능): 입력 마감. D-3 / D-1 / 당일 리마인더를 **미입력자에게만** 발송.
- 마감 후에도 입력·수정은 가능하다(잠그지 않는다). 다만 확정 후 변경 시 모임장에게 알림.

## 6. 날짜 처리 규칙 — 버그 방지
- 앱과 DB 모두 `YYYY-MM-DD` 문자열 / `date` 타입. **타임존 변환을 절대 하지 않는다.**
- `new Date("2026-08-01")`은 UTC 자정으로 파싱되어 KST에서 7월 31일이 된다. 사용 금지.
- 날짜 유틸은 `src/lib/date.ts` 한 곳에만 둔다. 다른 파일에서 `dayjs()`를 직접 부르지 않는다.
- 월 그리드 생성 시 주 시작은 **일요일**. 앞뒤 빈칸은 이전/다음 달 날짜를 흐리게 표시.
- 공휴일: 초기에는 넣지 않는다. 필요해지면 정적 JSON으로 연 1회 갱신(외부 API 의존 금지).

## 7. 동시성
- 가용성은 `upsert (member_id, date)` → 마지막 쓰기 승리. 본인 데이터이므로 충돌 무해.
- 날짜 확정은 `meetups.status = 'voting'` 조건부 업데이트로 한 번만 성공하게 한다.
- Realtime으로 다른 사람 입력이 들어오면 달력 게이지만 갱신하고, 내가 편집 중인 셀은 건드리지 않는다.

## 8. 테스트 케이스 (반드시 통과)
1. 6명 전원 `available` → 셀 배경 `availAll`(진한 연두), 게이지 대신 날짜만.
2. 5명 `available` + 1명 미등록 → `availHigh`(채도 뺀 연두). 4명이면 `availMid`, 3명이면 하이라이트 없음.
2-1. 아무도 입력하지 않은 달 → 하이라이트가 하나도 없다(달력이 통째로 칠해지지 않는다).
2-2. 불가를 누르면 그 기간 내 `available` 행이 사라지고 카운트가 그만큼 줄어든다.
3. 반복 규칙 "매주 수요일 불가"가 있는데 특정 수요일만 `available` 입력 → 개별 입력이 이긴다.
4. 모임장이 `unavailable`인 날은 추천 상위 3개에 절대 들어오지 않는다.
5. 월말/월초 경계(7/31, 8/1)에서 날짜가 하루 밀리지 않는다.
6. 멤버가 `is_active = false`가 되면 분모가 5로 줄고 기존 집계가 재계산된다.
7. 2월/윤년, 31일 없는 달에서 그리드가 깨지지 않는다.
