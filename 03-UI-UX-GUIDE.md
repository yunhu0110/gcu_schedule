# 03. UI/UX 가이드

> ⚠️ **방향 갱신 (ADR-008, 2026-07-27)**: 기본 방향이 「명조 월간지」 → **「Wanted Sans 하이에너지」**로 피벗됨.
> - 제목/브랜드: 명조 → **Wanted Sans**(현재 Noto Sans KR 900 스탠드인), 초대형 D-day = **Big Shoulders Display**.
> - 라운드: 작게(4/8) → **크게(카드16/소프트20/히어로26/버튼14)**.
> - **색·간격 팔레트는 그대로.** 명조(serif)와 neon은 **표지·과월호 감성 포인트에만** 남긴다.
> 아래 본문의 "라운드 작게·명조 대제목" 서술은 이 배너로 대체된다. 최신 구현값은 `src/theme/tokens.ts` + `10-DESIGN-BRIEF` 참조.

## 디자인 컨셉: 「월간지」
컨플루언스의 정보 구조 + **인쇄 잡지의 표지**.
매달 모임장이 바뀌는 구조를 그대로 잡지 편집장 개념으로 번역한다.
앱을 열면 이번 호 표지가 전면에 뜨고, 그 아래로 이번 달 기사(모임 일정)와
과월호 서가(위키)가 이어진다. `Vol. 2026.07` 같은 호수 표기가 앱 전체의 뼈대.

**이렇게는 안 만든다**: 카드가 둥둥 떠 있는 무색 대시보드, 그라데이션 히어로,
숫자 크게 + 라벨 작게 조합, 이모지로 성격을 대신하는 화면.

## 토큰 (`src/theme/tokens.ts` — 단일 출처)

### 색
| 토큰 | 값 | 용도 |
|---|---|---|
| `ink` | `#14161D` | 본문 텍스트, 헤어라인, 불가 상태 |
| `paper` | `#FFFFFF` | 기본 배경 |
| `mist` | `#EDECE8` | 판면(섹션 배경), 미입력 상태 |
| `cobalt` | `#2140E0` | 주 액션, **가능** 상태 |
| `neon` | `#E8318A` | 표지 강조, **전원 가능** 날짜 |
| `amber` | `#FFC53D` | **미정** 상태, 마감 임박 경고 |
| `slate` | `#7A7F8C` | 보조 텍스트, 캡션 |

- 이 7개 밖의 색은 쓰지 않는다. 투명도는 `ink12`, `ink08`처럼 토큰으로 파생시킨다.
- 모임장이 고르는 `theme_color`는 **표지 영역 안에서만** 적용된다. 앱 전체 색을 바꾸지 않는다.
- 다크 모드는 P3. 토큰은 처음부터 `light`/`dark` 두 세트를 담을 수 있는 구조로 만들되 지금은 light만 채운다.

### 타이포그래피
| 역할 | 서체 | 사용처 |
|---|---|---|
| Display | 국문 명조 극태 (`Noto Serif KR` 900 등) | 표지 이름, 화면 대제목. **절제해서 쓴다** |
| Body | `Pretendard` 400 / 600 | 모든 본문, 버튼, 라벨 |
| Mono | `IBM Plex Mono` 500 | 호수(`VOL. 2026.07`), 날짜, D-day, 금액, 카운터 |

스케일: `display 44/1.15` · `h1 26/1.3` · `h2 20/1.35` · `body 16/1.5` · `bodySm 14/1.5` · `caption 12/1.4` · `mono 12/1.2 (letterSpacing 1.2, 대문자)`

명조는 **표지와 화면 대제목에만**. 본문에 명조를 섞으면 컨셉이 무너진다.
폰트는 `expo-font`로 프리로드하고, 로드 전에는 스플래시를 유지한다.

### 레이아웃
- 8pt 그리드. 화면 좌우 여백 `20`. 섹션 간격 `32`.
- **라운드는 작게**: 카드 `4`, 버튼 `8`, 표지는 `0` (full-bleed).
- 그림자 대신 **1px 헤어라인**(`ink12`)으로 구획을 나눈다. 인쇄물의 괘선 감각.
- 섹션 머리에는 모노 대문자 눈썹 라벨 + 좌측 얇은 잉크 바.

### 시그니처 요소 1 — 표지
```
┌─────────────────────────────┐
│ V │                         │  ← 좌측 세로 모노 호수 (VOL. 2026.07)
│ O │   [모임장 사진 · 흑백    │
│ L │    + theme_color 오버레이]│
│ . │                         │
│ 0 │   이  달  의            │  ← 명조 극태, 2줄 좌측 정렬
│ 7 │   모 임 장              │
│   │   김 ○ ○                │
│   │  ─────────────────      │
│   │  "8월엔 성심당 재방문."  │  ← 모임장 한마디
└─────────────────────────────┘
```
- 사진은 흑백 처리 후 `theme_color` 멀티플라이 오버레이 → 6명 사진 화질이 달라도 톤이 통일된다.
- 앱 콜드 스타트 시 표지 제목에 **마스크 리빌 1회(600ms)**. 이게 이 앱의 유일한 연출.
  그 외 화면에는 애니메이션을 더하지 않는다. `prefers-reduced-motion` 시 리빌 생략.

### 시그니처 요소 2 — 6칸 게이지 달력 셀
멤버가 정확히 6명이라는 사실을 시각 언어로 쓴다. 채도로 뭉개지 않고 **칸을 센다.**
```
 ┌──────┐   ┌──────┐   ┌──────┐
 │  14  │   │  15  │   │  16  │
 │▓▓▓▓▓▓│   │▓▓▓▓░░│   │▓▓██░░│
 └──────┘   └──────┘   └──────┘
  전원가능    가능4/미입력2  불가2 포함
```
- 셀 하단 6칸 미니 게이지: `cobalt`=가능, `ink`=불가, `amber`=미정, `mist`=미입력.
- **전원 가능**한 날은 셀 전체를 `neon`으로 채우고 숫자를 흰색으로 반전. 한눈에 튄다.
- 불가가 1명이라도 있으면 게이지에 `ink` 칸이 보이므로 색약 사용자도 위치로 구분 가능.

## 인터랙션 원칙
- **입력은 한 손으로 3초.** 날짜 탭 1회 = 가능, 2회 = 불가, 3회 = 해제. 롱프레스 후 드래그로 범위.
- 상태 변경은 낙관적 업데이트 후 서버 반영. 실패 시 되돌리고 토스트로 알린다.
- 파괴적 동작(날짜 확정, 정산 마감)만 확인 시트. 나머지는 확인 없이 즉시 실행 + 되돌리기 제공.
- 하단 탭 5개: `표지` · `달력` · `위키` · `정산` · `나`
- 햅틱: 상태 토글에 `selection`, 확정에 `success`. 그 외엔 쓰지 않는다.

## 문구 규칙
- 버튼은 일어날 일을 그대로 쓴다. "제출" ❌ → "날짜 확정하기" ⭕. 확정 후 토스트도 "확정했어요"로 동일 어휘 유지.
- 시스템 용어 금지. "동기화 실패" ❌ → "저장하지 못했어요. 다시 눌러주세요" ⭕
- 빈 화면은 안내가 아니라 초대다. "아직 아무도 8월 일정을 넣지 않았어요. 첫 번째로 넣어볼까요?"
- 에러는 사과하지 않고, 무엇이 잘못됐고 무엇을 하면 되는지만 말한다.
- 존댓말 평서문, 문장부호 최소, 감탄사·이모지 남용 금지.

## 품질 하한선 (예외 없음)
- 최소 지원 폭 320pt까지 깨지지 않는다.
- 모든 터치 타깃 최소 44x44.
- 텍스트 대비 4.5:1 이상. `slate` on `mist` 조합은 캡션에만.
- 이미지에는 `accessibilityLabel`. 달력 셀에는 "8월 14일, 6명 모두 가능" 형태의 라벨.
- 로딩은 스피너 대신 스켈레톤(판면 색 `mist`)으로.
